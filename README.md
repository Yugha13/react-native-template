# React Native Template

A robust starter template for building React Native applications with Expo, NativeWind v4, and TypeScript.

## 🚀 Features

- **Expo**: The best way to build React Native apps.
- **NativeWind v4**: Utility-first CSS styling for React Native (Tailwind CSS).
- **TypeScript**: Strict type checking for better code quality.
- **Prettier & ESLint**: Automated code formatting and linting.
- **Absolute Imports**: Configured in `tsconfig.json`.

## 🛠️ Prerequisites

- [Node.js](https://nodejs.org/)
- [Bun](https://bun.sh/) (optional, but recommended) or npm/yarn
- [Expo Go](https://expo.dev/client) app on your iOS/Android device

## 📦 Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/Yugha13/react-native-template.git
   cd react-native-template
   ```

2. Install dependencies:
   ```bash
   npm install
   # or
   bun install
   ```

## 🏃‍♂️ Running the App

Start the development server:

```bash
npx expo start
```

- Press `a` for Android emulator
- Press `i` for iOS simulator
- Scan the QR code with Expo Go on your physical device

## 📝 Scripts

- `npm start`: Start Expo server
- `npm run android`: Run on Android
- `npm run ios`: Run on iOS
- `npm run web`: Run on Web
- `npm run lint`: Lint files
- `npm run format`: Format files

## 🤝 Contributing

Contributions are welcome! Please open an issue or submit a pull request.
